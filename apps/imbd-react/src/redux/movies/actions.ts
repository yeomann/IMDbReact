import { IMoviesRedux } from '../@types/movies.redux.types';

const actions: IMoviesRedux = {
  SET_FAV: 'movies/SET_FAV',
  GET_FAV: 'movies/GET_FAV',
  SET_WATCHL: 'movies/SET_WATCHL',
  GET_WATCHL: 'movies/GET_WATCHL',
};

export default actions;
